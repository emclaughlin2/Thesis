\subsection{Pileup Studies}
\label{sec:pileup_studies}

To study the effect of pileup on the UE measurement, we first classify pile up rates run-by-run for full set of good runs in 0 mrad + 1.5 mrad dataset using the run integrated MBD live rate.
The average pile up rate calculation assumes that simultaneous collisions are given by a Poisson distribution: $P(k) = (\lambda^{k}e^{-\lambda})/k!$ where $\lambda$ is the average number of collisions per bunch crossing.
Therefore, $\lambda$ can be calculated as $\lambda = f_{c}/(f_{b}*N)$ where $f_{c}$ is the run integrated MBD live rate, determined run-by-run by the number of MBD live counts divided by the length of the run in seconds, the bunch crossing rate is $f_{b} = 78 kHz$ and bunch number $N = 111$. 

The average pileup rate follows as:
\begin{equation}
   \langle PU \rangle = \frac{P(k>1)}{P(1)+P(k>1)}
\end{equation}
Here the average pileup rate is calculated from the triggered dataset; therfore this quantity can be understood as "given a crossing with a collision, what is the probability of that crossing having more than one collision".
The MBD live rates range run-by-run from 60kHz-2MHz across the 0 mrad + 1.5 mrad dataset with the largest variation in the MBD live rates in the 0 mrad dataset.
These rates correspond to run-by-run average pile up values of $< 1\%$ to $> 10\%$. 
The average pileup value and MBD live rate for each run are shown in Figure~\ref{fig:pileup_runbyrun}. 

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{Figures/pileup/run_by_run_pileup.png}
    \caption{Run-by-run average pileup (top) and MBD live rate (bottom) for 0 mrad + 1.5 mrad dataset.}
    \label{fig:pileup_runbyrun}
\end{figure}

For this study, nine pile up ranges are used to determine the level of pileup effects on the UE measurement.
These ranges are $\langle PU\rangle < 2\%$, $2\% < \langle PU\rangle < 3\%$, $3\% < \langle PU\rangle < 4\%$,
 $4\% < \langle PU\rangle < 5\%$, $5\% < \langle PU\rangle < 6\%$, $6\% < \langle PU\rangle < 7\%$, 
 $7\% < \langle PU\rangle < 8\%$, $8\% < \langle PU\rangle < 9\%$, $9\% < \langle PU\rangle < 10\%$.
 Values above 10\% are not included in this study.
The histogram of runs as a function of average pileup for the 0 mrad + 1.5 mrad combined dataset is shown in Figure~\ref{fig:avg_pileup}.

\begin{figure}
    \centering
    \includegraphics[width=0.7\linewidth]{Figures/pileup/avg_pileup.png}
    \caption{Histogram of runs as a function of average pileup for 0 mrad + 1.5 mrad combined dataset.}
    \label{fig:avg_pileup}
\end{figure}

The UE measurement is then studied as a function of pileup both in the 0 mrad and 1.5 mrad datasets separately and in combination. 
The average $\Sigma E_T$ is calculated for each pileup range for each dataset and a linear fit is performed to the average $\Sigma E_T$ for each pileup range for the combined dataset.
The slope of the linear fit is then used to determine the effect of pileup on the UE measurement.
The average $\Sigma E_T$ as a function of pile up and the linear fit are shown in Figure~\ref{fig:pileup_0mrad_1.5mrad_separately_together}.

The combined dataset agrees fairly well with the linear fit indicating that the effect on the transverse region $\Sigma E_T$ can be described by a linear function on $\langle PU \rangle$
and a simple subtraction of the linear fit from the average $\Sigma E_T$ can be used to correct for the pileup effect.
There are some small deviations from the linear fit at high pileup which are expected as the MBD trigger becomes non-linear at high pileup.
However for this analysis only runs with average pileup $<6\%$ are included in the final measurement, therefore this non-linearity correction at high pileup does not affect the final measurement.
Additionally, the 1.5 mrad and 0 mrad datasets agree well in the region they overlap, indicating that the pileup effect on the UE measurement is similar in the 0 mrad and 1.5 mrad datasets.

\begin{figure}
    \centering
    \begin{subfigure}[t]{0.48\linewidth}
        \includegraphics[width=\linewidth]{{Figures/pileup/pileup_0mrad_1.5mrad_separately}.png}
        \caption{Average $\Sigma E_T$ density for 0 mrad dataset (open) and 1.5 mrad dataset (closed)}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\linewidth}
        \includegraphics[width=\linewidth]{{Figures/pileup/pileup_0mrad_1.5mrad_together_dijet_w_bkgcuts}.png}
        \caption{Average $\Sigma E_T$ for 0 mrad + 1.5 mrad dataset (closed)}
    \end{subfigure}
    \caption{Average $\Sigma E_T$ density and $\Sigma E_T$ as a function of pileup for 0 mrad (open) + 1.5 mrad (closed) datasets separately (left) and combined (right).}
    \label{fig:pileup_0mrad_1.5mrad_separately_together}
\end{figure}

\subsection{Pileup Application}
\label{sec:pileup_application}

Pileup effects are studied by examining the relationship between the average pileup proxy, determined on a run-by-run basis, and the transverse region average total $E_{T}$. 
Figure~\ref{fig:pileup_0mrad_1.5mrad_separately_together} shows a roughly linear increase in transverse region $E_{T}$ with increasing average pileup, with deviations at high pileup where the MBD trigger efficiency becomes non-linear.
A pileup correction, derived from the slope of the linear fit to the transverse region average total $E_{T}$ of clean dijet events shown in Figure~\ref{fig:pileup_0mrad_1.5mrad_separately_together}, is applied to the reconstructed transverse region $E_{T}$ prior to unfolding. 
Pileup corrections derived from clean inclusive jet events (jets passing the energy fraction cut and timing cuts) were also derived and found to be consistent with the pileup correction derived from clean dijet events.

Figure~\ref{fig:pu_reco_unfolded} compares reconstructed and unfolded distributions with and without the pileup correction. 
Small differences are observed in the $E_{T}$ distribution at the reconstructed level and the effect on the mean of the reconstructed $E_{T}$ distribution shown in the legends of Figure~\ref{fig:pu_reco_unfolded} is also relatively small.
The effect on the unfolded mean transverse region $E_{T}$ is $<4\%$ for all leading jet $p_{T}$ bins in the measurement range.  

As a result, the final measurement uses the results with the pileup correction applied and as a systematic uncertainty on the effect of pileup this pileup correction is toggled off.
The observed difference between the pileup correction on and off is treated as a systematic uncertainty, shown for an unfolded distribution in Figure~\ref{fig:pu_reco_unfolded}, and is found to have at most a $4\%$ effect on both the reconstructed and unfolded average transverse region $E_{T}$.
 
\begin{figure}
    \centering
    \begin{subfigure}[t]{0.48\linewidth}
        \includegraphics[width=\linewidth]{Figures/pileup/pileup_effects_reco_inclusive.png}
        \caption{Reconstructed transverse region $E_{T}$ distribution for inclusive jet events with and without pileup correction.}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\linewidth}
        \includegraphics[width=\linewidth]{Figures/pileup/pileup_effects_reco_dijet.png}
        \caption{Reconstructed transverse region $E_{T}$ distribution for dijet events with and without pileup correction.}
    \end{subfigure}
    \begin{subfigure}[t]{0.48\linewidth}
        \includegraphics[width=\linewidth]{Figures/pileup/pileup_effects_unfolded_inclusive.png}
        \caption{Unfolded transverse region $E_{T}$ distribution for inclusive jet events with and without pileup correction.}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\linewidth}
        \includegraphics[width=\linewidth]{Figures/pileup/pileup_effects_unfolded_dijet.png}
        \caption{Unfolded transverse region $E_{T}$ distribution for dijet events with and without pileup correction.}
    \end{subfigure}
    \caption{Reconstructed (top) and unfolded (bottom) transverse region $E_{T}$ distributions for inclusive jet (left) and dijet (right) events with and without the pileup correction.}
    \label{fig:pu_reco_unfolded}
\end{figure}